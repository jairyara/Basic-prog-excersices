let persons = ['Jair', 'Juan', 'Teresa', 'Alejandro'];
let salary = [5000, 4200, 2000, 100];

for (let i = 0; i < persons.length; i++) {
    console.log(persons[i] + ' tiene un salario de ' + salary[i]);
}